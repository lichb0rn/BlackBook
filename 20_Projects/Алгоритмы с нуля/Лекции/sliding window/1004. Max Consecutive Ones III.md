---
created: 2026-02-10
tags:
  - type/note
source: https://leetcode.com/problems/max-consecutive-ones-iii/description/
related:
area:
up:
---
## Итерация 1
```go
func longestOnes(nums []int, k int) int {
    begin := 0
    // сколько 0 мы видели
    windowState := 0
    result := 0

    for end := range len(nums) {
       if nums[end] == 0 {
            windowState++
       }
    
        // сжимаем, когда кол-во нулей > k 
        for windowState > k {
            if nums[begin] == 0 {
                windowState--
            }

            begin++ // shrink window
        }

        result = max(result, end - begin + 1)
    }

    return result  
}

func max(a, b int) int {
	if a > b {
		return a
	}
	return b
}
```