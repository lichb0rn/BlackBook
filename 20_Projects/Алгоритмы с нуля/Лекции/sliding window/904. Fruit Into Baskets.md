---
created: 2026-02-10
tags:
  - type/note
source: https://leetcode.com/problems/fruit-into-baskets/description/
related:
area:
up:
---
## Итерация 1
```go
func totalFruit(fruits []int) int {
    begin := 0
    // сколько фруктов видели
    windowState := make(map[int]int)
    // длина подмассива
    result := 0

    for end := range len(fruits) {
        windowState[fruits[end]]++

        for len(windowState) > 2 {
            _, ok := windowState[fruits[begin]]
            if ok {
                windowState[fruits[begin]]--

                if windowState[fruits[begin]] == 0 {
                    delete(windowState, fruits[begin])
                }
            }
            begin++
        }

        result = max(result, end - begin + 1)
    }

    return result    
}

func max(a, b int) int {
	if a > b {
		return a
	}
	return b
}
```