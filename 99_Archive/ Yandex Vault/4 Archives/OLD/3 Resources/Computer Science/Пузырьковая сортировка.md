---
aliases: ["bubble sort", "пузырьковая сортировка", "сортировка пузырьком"]
---
MOC:  [[Алгоритмы сортировки|Сортировка]]

---

**Худшее/среднее время:** $O(n^2)$ 
**Лучшее время:** $O(n)$ - только в случае уже отсортированного массива

## Код

```swift
public func bubbleSort<T>(_ collection: inout T) 
			where T: MutableCollection, T.Element: Comparable {
    
    guard collection.count >= 2 else {
        return
    }
    
    for end in collection.indices.reversed() {
        var swapped = false
        var current = collection.startIndex
        
        while current < end {
            let next = collection.index(after: current)
            
            if collection[current] > collection[next] {
                collection.swapAt(current, next)
                swapped = true
            }
            
            current = next
        }
        
        if !swapped {
            return
        }
    }
}
```