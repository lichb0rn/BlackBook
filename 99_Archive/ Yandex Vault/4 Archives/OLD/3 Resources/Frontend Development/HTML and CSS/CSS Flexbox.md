---
aliases: ["flexbox"]
---

# Flexbox
Для активации `flexbox`  в  [[HTML and CSS|css]] используется `display: flex;` на контейнере:

```css
#container {
	display: flex;
}
```

## flex-direction
`flex-direction` позволяет выравнять дочерние элементы в ряды (default) или колонки. 
Возможные значение:
- `row` - в ряд (default)
- `column`
- `row-reverse`
- `column-reverse`

## justify-content
`justify-content` служит для выравнивая элементов внутри контейнера по main axis.
[Схема осей](https://www.w3.org/TR/css-flexbox-1/images/flex-direction-terms.svg)
Опции:
- `center` - выравнивает по центру
- `flex-start` - выравнивает с начала контейнера. Для ряда - по левому краю, для колонки - кверху.
- `flex-end` - противоположно `flex-start`
-  `space-between` - выравнивает к центру с дополнительным пустым местом между элементами. Первый и последней элемент прижаты к началу и концу контейнра соответственно.
-  `space-around` - похоже на `space-between`, но первый и послдней элемент не прижаты к краям контейнера. Пустое пространство равномерно распределено между всем элементами.
-  `space-evenly` 
![[Pasted image 20220817121239.png]]

## align-items
`align-items` - схоже с `justify-content`, но работает на cross axis.
- `flex-start` и `flex-end` - выравнивает по началу/концу контейнера. Для рядов - кверху/низу, для колонок - по левому/правому краю.
- `center` - по центру
- `stretch` (default) - растягивает элементы, чтобы заполнить контейнер
- `baseline` - по baseline текста
![[Pasted image 20220817122148.png]]

## align-content
Схоже с `justify-content`, но работает только с несколькими строками. Для одной строки эффекта не будет.
- ![[Pasted image 20220817121633.png]]

## flex-wrap
По умолчанию `flex` контейнер будет стараться разместить все элементы вместе, для рядов - на одной линии. `flex-wrap` - позволяет задать правила переноса.
`nowrap` (default) - не переносить
`wrap` - перенести на несколько линий. Для рядов - сверху -> вниз, для колонок - справа -> налево
`wrap-reverse` - противоположно `wrap`

## flex-shrink и flex-grow
Свойство `flex-shrink` на элементе позволяет блоку сжиматься, если `flex` контейнер мал
```css
<style\>
#box-container {
	display: flex;
	height: 500px;
}

#box-1 {
	background-color: dodgerblue;
	width: 100%;
	height: 200px;
	flex-shrink: 1;
}
 
#box-2 {
	background-color: orangered;
	width: 100%;
	height: 200px;
	flex-shrink: 2;
}
</style\>

<div id\="box-container"\>
	<div id\="box-1"\></div\>
	<div id\="box-2"\></div\>
</div\>
```
`box-2` сожмётся в 2 раза сильнее `box-1`.

`flex-grow` - противолопожно `flex-shrink`.

## flex-basis
`flex-basis` определяет начальный размер элемента перед тем, как CSS применит `flex-shrink` и `flex-grow`. Задаётся в размерностях (`px`, `em`, `%`, etc).

После того, как `flex-basis` определён, браузер попытается увеличить размер элемент в контейнере `flex`. Скорость, с которой браузер будет это делать зависит от `flex-grow`.
По умолчанию `flex-grow = 0`, что означает запрет на рост внутри контейнера, элемент должен оставаться в своём базисном размере.
![[Pasted image 20220817092024.png]]

## Shortcut
`flex` для элемента позволяет задать все свойства элемента сразу.
`flex: 2 1 150px;` - задает `flex-grow: 2`, `flex-shrink: 1` и `flex-basis: 150px`. Default: `flex: 0 1 auto;`.
```css
#box-container {
	display: flex;
	height: 500px;
}

#box-1 {
	background-color: dodgerblue;
	flex: 2 2 150px;
	height: 200px;
}

#box-2 {
	background-color: orangered;
	flex: 1 1 150px;
	height: 200px;
}
 ```
 Если контейнер > 300px, но box-1 будет в 2 раза больше box-2, если < 300px, то в 2 раза меньше.
 
 ## order
 `order` позволяет задать порядок элементов. По умолчанию элементы расположены в порядке разметки в html. По умолчанию $0$.
 Пример:
```css
section#topics article:nth-last-of-type(2n + 2) {
	order: -1;
}
```

Всё нечетные элементы будут расположены впереди четных.

 ## align-self
 `align-self` позволяет определить для каждого элемента индивидуально (свойства `float`, `clear`, `vertical-align` ==не работают== для `flex` элементов).
 Значения аналогичны `align-items` и перезаписывают их.


--- 

## 📇 Additional Metadata

- 🗂 Type:: #type/note