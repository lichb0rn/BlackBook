---
uuid: 20210712152405
created: 2021-07-12T15:24:05
alias: 
---

# [[CSS Grid]]

Включается с помощью `display: grid;` для элемента-контейнера в [[HTML and CSS|html]].

## Колонки

Размер и количество колонок можно задать с помощью свойства `grid-template-columns`:

```css
.container {
	display: grid;
	grid-template-columns: 50px 50px;
}
```

Включает 2 колонки по 50 пикселей, количество и размер строк при этом будет выведен автоматически.

## Строки

Строки задаются аналогично с помощью `grid-template-rows: 50px 50px;`.

## Отступы

Отступы можно задать с помощью `grid-column-gap: 10px;` и `grid-row-gap: 5px;`.
Или шортката: `grid-gap: 10px 20px;` - отступ 10 пкс между строками и 20 между колонками.

### Размер элемента

Свойство `grid-column` и `grid-row` позволяют задать количество колонок/строк, которое займёт элемент:

```css
.item {
	grid-column: 2 / 4;
}
```

Элемент займёт колонки 2 и 3.

### Выравнивание

Выравнивание осуществляется с помощью `justify-self` и `align-sellf`:

- `start
- `end`
- `center`
  Свойства приведенные выше задаются для каждого элемента, но также можно задать их для всех элементов на уровне контейнера с помощью `justify-items` и `align-items`.

### Grid Area

С помощью свойства `grid-template-areas` задать струкутру сетки и проименовать ячейки:

```css
.container {
	font-size: 40px;
	min-height: 300px;
	width: 100%;
	background: LightGray;
	display: grid;
	grid-template-columns: 1fr 1fr 1fr;
	grid-template-rows: 1fr 1fr 1fr;
	grid-gap: 10px;
	grid-template-areas:
		'header header header'
		'advert content content'
		'footer footer footer';
}

.item5 {
	background: PaleGreen;
	grid-area: footer;
}
```

Пример вышел располагает `.item5` в виде footer на всю строку.

---

---

## 📇 Additional Metadata

- 🗂 Type:: #type/note

Дополнительные ссылки:
https://css-tricks.com/things-ive-learned-css-grid-layout/
