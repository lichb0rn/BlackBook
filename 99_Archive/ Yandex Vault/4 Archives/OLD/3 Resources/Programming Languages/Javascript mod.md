2021-03-07 09:57
Tags: #"" - [[Math]] - [[JavaScript]]
\_\_

# Деление по модулю

В [[JavaScript]] оператор `%` считает остаток от деления. Такое поведение отличается от некоторых других языков, например, [[Python]].

## Код

```js
function modulo(a, n) {
	return ((a % n) + n) % n;
}
```
