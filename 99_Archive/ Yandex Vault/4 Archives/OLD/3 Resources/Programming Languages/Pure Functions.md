---
uuid: 20220725201134
created: 2022-07-25T20:11:34
aliases:
  - чистота функции
  - чистые функции
---

# [[Pure Functions]]

Pure function (или чистая функция) обладает двумя свойствами:
1. является детерминированной
2. не обладает побочными действиями (side effect).

## Детерминированность функции
Детерминированность означает, что функция возвращает всегда один и тот же результат, если на вход поступают одинаковые данные.

Функция не зависит от внешнего состояния в программе.

## Побочные действия
В императивных языках некоторые функции в процессе выполнения своих вычислений могут модифицировать значения глобальных переменных, осуществлять операции ввода-вывода, реагировать на исключительные ситуации, вызывая их обработчики. Такие функции называются **функциями с побочными эффектами**. 

Другим видом побочных эффектов является модификация переданных в функцию параметров (переменных), когда в процессе вычисления выходного значения функции изменяется и значение входного параметра.

### Список побочных действий на примере [[JavaScript]]
- http запрос
- изменение данных
- вывод на экран/консоль
- манипуляции с [[DOM]]
- `Math.random()`
- Получение текущей даты/времени.



---

## 📇 Additional Metadata

- 🗂 Type:: #type/note
- 🏷️ Tags:: [[Functional Programming]]