---
uuid: 20220721093007
created: 2022-07-21T09:30:07
alias: docker cli
---

# [[Ключевые команды Docker]]

`docker build` - собрать образ
`docker run` - запустить образ в качестве контейнера
`docker commit` - сохранить контейнер в качестве образа
`docker tag` - присвоить тег образу Docker

## Запуск контейнера
`docker run -i -t -p 8000:8000 --name example1 todoapp`
- `-p` - перенаправляет порт 8000 в 8000
- `--name` - присваивает контейнеру уникальное имя

`docker start example1` - перезапуск контейнера `example1`

### Запуск контейнера в фоне
Контейнеры Docker, как и большинство процессов, будут запускаться по умолчанию на переднем плане. Наиболее очевидный способ запуска контейнера Docker в фоновом режиме – это использование стандартного оператора управления `&`. Хотя это и сработает, вы можете столкнуться с проблемами, если выйдете из сеанса терминала, что потребует использования флага `nohup`, создающего файл в вашем локальном каталоге с выводом, которым вы должны управлять ... У вас есть идея: гораздо лучше применить для этого функциональность демона Docker - флаг `-d`:
`docker run -d -i -p 1234:1234 --name daemon ubuntu:14.04 nc -l 1234`
`-i` даёт возможность подключаться к контейнеру

### Перезапуск контейнера
`docker run --restart=[no,always,unless-stopped,on-failure]`

## Просмотр состояния всех контейнеров
`docker ps` выводит список всех запущенных контейнеров
```bash
➜  todo docker ps
CONTAINER ID   IMAGE     COMMAND                  CREATED         STATUS         PORTS                    NAMES
15b299be5aa8   todoapp   "docker-entrypoint.s…"   6 minutes ago   Up 2 seconds   0.0.0.0:8000->8000/tcp   example1
```


`docker ps -a` - будет выведен список всех запущенны контейнеров:
```shell
➜  todo docker ps -a
CONTAINER ID   IMAGE                   COMMAND                  CREATED         STATUS                      PORTS     NAMES
15b299be5aa8   todoapp                 "docker-entrypoint.s…"   3 minutes ago   Exited (1) 18 seconds ago             example1
60cc449d8309   node:16                 "docker-entrypoint.s…"   7 days ago      Exited (0) 7 days ago                 wowbot
c5e6021f724b   node:16                 "docker-entrypoint.s…"   7 days ago      Exited (137) 7 days ago               wowapi-example-1
173bdedc5b12   lichb0rn/wowbot:0.1.3   "docker-entrypoint.s…"   8 days ago      Exited (255) 7 days ago               cool_cartwright
5b179dfdba6b   lichb0rn/wowbot         "docker-entrypoint.s…"   13 days ago     Exited (137) 13 days ago              mystifying_banzai
```



---

## 📇 Additional Metadata

- 🗂 Type:: #type/note