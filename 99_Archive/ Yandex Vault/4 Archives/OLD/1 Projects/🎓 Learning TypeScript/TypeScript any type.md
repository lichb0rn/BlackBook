---
uuid: 20230906200237
created: 2023-09-06T20:02:37
aliases:
  - any
---

# [[TypeScript any type]]

 ## any
 В TypeScript в момент компиляции у всего должен быть тип, и `any` становится типом по умолчанию там, где программист и [[TypeScript]] (модуль проверки) не могут точно определить тип элемента. Можно охарактеризовать его как тип крайнего случая. 
 `any` же представляет собой набор всех возможных значений, и вы можете делать с ним все что угодно: прибавлять к нему, умножать на него, вызывать для него `.pizza()` и т.д.
 ```ts
let a: any = 666 // any
let b: any = ['danger'] // any
let c = a + b // any
```
Когда [[TypeScript]] выводит тип некоего значения как any (например, вы забыли аннотировать параметр функции или импортировали нетипизированный [[JavaScript]]-модуль), то в процессе компиляции он выбросит исключение и подчеркнет ошибку красным. Используя же явную аннотацию `a` и `b` типом `any` (`:any`), вы избежите исключения, потому что таким образом дадите [[TypeScript]] понять осознанность этого действия.

По умолчанию [[TypeScript]] не жалуется на значения, для которых он вывел тип `any` (неявные any). Чтобы активировать функцию защиты от неявных any, нужно добавить флаг noImplicitAny в `tsconfig.json`.
	`noImplicitAny` становится активна при включении режима `strict` в `tsconfig.json`


---

## 📇 Additional Metadata
- 🛠️ Status:: 
- 🗂 Type:: #type/note
- ℹ️ Source::  [[Профессиональный TypeScript]]
- 🏷️ Tags::  [[Система типов TypeScript]]