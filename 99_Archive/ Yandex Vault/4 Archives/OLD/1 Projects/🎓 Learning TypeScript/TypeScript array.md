---
uuid: 20230911093954
created: 2023-09-11T09:39:54
aliases:
  - массивы
  - arrays
---
# [[TypeScript array]]

```ts
let a = [1, 2, 3];
let b: string[] = ['a'];
let c = [1, 'a'] // (string | number)[]

let f = ['red']
f.push('blue')
f.push(true) //Argument of type 'boolean' is not assignable 
			// to parameter of type 'string'.ts(2345)

let g = []; // any[]
g.push(1); // number[]
g.push('red') // (string | number)[]
```
[[TypeScript]] поддерживает два варианта синтаксиса для массивов: `T[]` и `Array<T>`. Они идентичны по значению и действию.

Когда вы инициализируете пустой массив, [[TypeScript]] не знает, какой тип будут иметь его элементы, и присваивает ему тип any. По мере добавления в массив новых значений [[TypeScript]] постепенно определяет его тип в соответствии с ними. Как только массив выйдет за определенный диапазон (например, если вы объявили его в функции, а затем вернули), тогда [[TypeScript]] присвоит ему последний тип, который не может быть расширен далее:
```ts
function buildArray() {
	let a = []; // any
	a.push(1); // number[]
	a.push('x'); // (string | number)[]
	return a;
}
let myArray = buildArray(); // (string | number)[]
myArray.push(true); // Argument of type 'boolean' is not assignable 
					// to parameter of type 'string | number'.ts(2345)
```


---

## 📇 Additional Metadata
- 🛠️ Status:: 
- 🗂 Type:: #type/note
- ℹ️ Source:: [[Профессиональный TypeScript]]
- 🏷️ Tags:: [[Система типов TypeScript]]