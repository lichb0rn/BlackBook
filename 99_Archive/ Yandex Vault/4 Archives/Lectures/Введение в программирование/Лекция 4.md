---
uuid: 20220107140758
created: 2022-01-07T14:07:58
alias:
- порядковая статистика
- quickselect
---

# [[Лекция 4]]

## Поиск k-й порядковой статистики

- k-я порядковая статистика - это k-й по величине элемент в массиве.
  Задача поиска можно свести к сортировке: сортируем массив по возрастанию и возвращаем k-й элемент. В данном случае время работы - $O(n \ log \ n)$.

Обозначение: пусть дан массив $a_1, a_2, a_3, ..., a_n$. Тогда k-я статистика - $a_{(k)}$.
Пример:

- массив `[1, 3, 5, 7, 2, 3, 4, 7]`. После сортировки: `[1, 2, 3, 3, 4, 5, 7, 7]`.
  Пусть $k = 5$. Тогда 5-я порядковая статистика - 5: $a_{(5)}=4$.

Алгоритм [[QuickSelect]] позволяет найти k-ю порядковую статистику за $O(n)$:

```
QuickSelect(A, k) {
	if len(A) == 1 {
		return A[0]
	}
	let x // случайный элемент из A

	// разбивает массив на 3 части
	// элементы [< x], [= x] и []> x]
	// B - элементы < x
	// C - элементы > x
	B, C = Partition(A, x)

	let n1, n2, n3 // количество чисел в частях массива после Partition()

	if k <= n1 {
		return QuickSeelect(B, k)
	} else k <= n1+ n2 {
		return x
	} else {
		return QuickSelect(C, k - n1 - n2)
	}
}
```

==Теорема==

- алгоритм QuickSelect в среднем ([[Математическое ожидание дискретной случайной величины|мат.ожидание]]) работает за $O(n)$.

## Детерминированный [[QuickSelect]]

- Медиана массива длины $n$ - это $a_{(n/2)}$.
  Пример: `[3, 1, 2, 5, 4]` Тогда медиана - 3.

```
kth(A , k) {
	// делим массив на блоки по 5 элементов
	// тогда B = [b1, b2, b3...] - медианы этих блоков
	m = kth(B, n/10) // ищем медиану массива B
	C, D = Partition(A, m)
	let n1, n2, n3 // длины C, C+m, D
	if k <= n1 {
		return kth(C, k)
	} else if k <= n1 + n2 {
		return m
	} else {
		kth(D, k - n1 - n2)
	}
}
```

## Детерминированный [[Быстрая сортировка|quick sort]]

В качестве `pivot` выбираем медиану, а не случайное число.

## Сортировка чисел

[[Поразрядная сортировка|radix]]

## Стабильная сортировка чисел

Стабильный алгоритм сортировки - алгоритм, в котором после сортировки сохраняют относительный порядок ( если $a_n$ идет перед $a_k$, b и они равны, то после сортировки порядок должен сохранится).

## Сортировка больших чисел

Сортировка подсчетом имеет ограничения на максимальное число. Числа $>10^6$ сложно сортировать, память может не хватить.
Для этого есть другой метод, основанный на сортировке пар.

Допустим у нас есть массив больших чисел: $a_1, a_2, ..., a_n$.
Предположим у них у всех одинаковая длина десятичной записи, а если нет - дополним в начале нулями.
Тогда $a_i$ можно представить как $(d_{1i}, d_{2i}, ..., d_{19i})$.

```
for j = 19 to 1 {
	StableSort по j-му разряду
}
```

Время работы - $O(19n)$. Сортировка LSD.


---

## 📇 Additional Metadata

- 🗂 Type:: #type/note
- ℹ️ Source::  https://www.youtube.com/watch?v=jEcJH3RiVVE
- 🏷️ Tags:: [[Алгоритмы сортировки]]