---
uuid: 20230906200412
created: 2023-09-06T20:04:12
aliases:
  - unknown
---

# [[TypeScript unknown type]]

В случае, когда есть значение, чей тип можно будет узнать позднее, вместо [[TypeScript any type|any]] примените `unknown`. Он представляет любое значение, но чтобы использовать это значение, [[TypeScript]] потребует уточнить его тип.

```ts
let a: unknown = 30 // unknown
let b = a === 123   // boolean
let c = a + 10      // Ошибка TS2571: объект имеет тип 'unknown'.
if (typeof a === 'number') {
	let d = a + 10  // number
}
```
1.	TypeScript никогда не выводит `unknown` — этот тип нужно явно аннотировать (`a`).
2.	Можно сравнивать значения со значениями типа `unknown` (`b`).
3.	Нельзя производить действия на основе предположения, что значение `unknown` имеет конкретный тип (`c`). Сначала нужно показать [[TypeScript]] наличие этого типа (`d`).

---

## 📇 Additional Metadata
- 🛠️ Status:: 
- 🗂 Type:: #type/note
- ℹ️ Source:: [[Профессиональный TypeScript]]
- 🏷️ Tags::  [[Система типов TypeScript]]