---
uuid: 20230917202602
created: 2023-09-17T20:26:02
alias:
---

# [[Функции в TypeScript]]

Пример:
```ts
function add(a: number, b: number): number {
	return a + b;
}
```
Возвращаемый тип подлежит выводу, но при желании его можно аннотировать явно.

[[TypeScript]] поддерживает те же способы объявления, что и [[Функции в JavaScript|JavaScript]].

## Параметры по умолчанию и опциональные параметры

Как и для [[TypeScript object|object]] и [[TypeScript tuples|кортежей]], можно использовать `?` для обозначения параметра в качестве опционального:
```ts
function log(message: string, userId?: string) {
	let time = new Date().toLocaleTimeString();
	console.log(time, message, userId || 'Not signed in');
}
```
С параметрами по умолчанию:
```ts
function log(message: string, userId = 'Not signed in') {
	let time = new Date().toLocaleTimeString();
	console.log(time, message, userId);
}
```
Когда мы присваиваем `userId` значение по умолчанию, то удаляем его опциональную аннотацию `?`. Больше нет необходимости его типизировать. [[TypeScript]] вполне способен вывести тип параметра на основе его значения по умолчанию.

## Rest параметры и arguments

Иногда вместо API функции с фиксированной арностью вы выберете API с переменным числом аргументов. Традиционно для этого используется магический [[JavaScript]]-объект `arguments`.

Среда выполнения [[JavaScript]] автоматически определяет `arguments` в функциях и присваивает ему список переданных функции аргументов. Поскольку `arguments` является не настоящим массивом, а всего лишь его подобием, то сначала необходимо преобразовать его в массив и лишь затем вызвать для него встроенный метод `.reduce`:
```ts
function variadicSum(): number {
	return Array
		.from(arguments)
		.reduce((total, n) => total + n, 0);
}
```
Однако при использовании `arguments` возникает большая проблема: небезопасность. [[TypeScript]] вывел типы `n` и `total` как `any`, молча позволив их передать. На ошибку же он укажет, только когда вы попытаетесь использовать `variadicSum`:
![[Pasted image 20230919172039.png]]

![[Pasted image 20230919172103.png]]

Поскольку мы не объявили, что `variadicSum` получает аргументы, [[TypeScript]] решает, что она их вообще не получает, отсюда и ошибка [[TypeScript TypeError|TypeError]]. Вместо `arguments` можно использовать [[JavaScript rest-параметры]]:
```ts
function variadicSum(...numbers: number[]): number {
	return numbers.reduce((total, n) => total + n, 0);
}
```
Функция может иметь не более одного оставшегося параметра, и этот параметр должен быть последним в списке.

В дополнение к вызову функции со скобками `()` [[JavaScript]] поддерживает [[JavaScript методы call, apply и bind|методы call, apply и bind]].

## Перегрузка функции
Перегруженная функция - функция с несколькими сигнатурами вызовов.
Например, спроектируем API для бронирования отпуска — назовем его Reserve. Начнем с зарисовки его типов (с полной сигнатурой):
```ts
type Reserve = {
	(from: Date, to: Date, destination: String): Reservation;
};
```
Теперь подытожим реализацию `Reserve`:
```ts
let reserve: Reserve = (from, to, destination) => { ... }
```
Мы можем видоизменить API для поддержки рейсов в один конец:
```ts
type Reserve = {
	(from: Date, to: Date, destination: string): Reservation;
	(from: Date, destination: string): Reservation;
};
```
При попытке запустить код [[TypeScript]] выдает ошибку в месте, где вы реализуете `Reserve`:
```ts
Type 
	(from: Date, 
	to: Date, 
	destination: string) => 
	{ from: Date; to: Date; hotel: string; }
is not assignable to type 'Reserve'.  
Target signature provides too few arguments. Expected 3 or more, but got 2.ts(2322)

let reserve: Reserve
```
Это происходит из-за особенности работы перегрузки сигнатуры в [[TypeScript]]. Если вы объявите набор сигнатур перегрузки для функции `f`, то с точки зрения вызывающего компонента тип `f `— это объединение сигнатур перегрузок. Но с позиции реализации `f` должен быть один скомбинированный тип, который может быть реализован. Вам придется самостоятельно объявить эту комбинированную сигнатуру вызова при реализации `f`. В примере с `Reserve` можете обновить функцию `reserve` так:
```ts
type Reserve = {
	(from: Date, to: Date, destination: string): Reservation;
	(from: Date, 
	toOrDestination: Date | string, 
	destination?: string): Reservation;
};

let reserve: Reserve = 
	(from: Date, 
	toOrDestination: Date | string, 
	destination?: string) => {
	
	if (toOrDestination instanceof Date && 
		destination !== undefined) {
		return { ... };
	} else if (typeof toOrDestination === 'string') {
		return { ... };
	}
	return { ... };
};
```
Перегруженные сигнатуры вызовов — это естественный способ моделирования работы `createElement`:
```ts
type CreateElement = {
	(tag: 'a'): HTMLAnchorElement
	(tag: 'canvas'): HTMLCanvasElement 
	(tag: 'table'): HTMLTableElement 
	(tag: string): HTMLElement
}
let createElement: CreateElement = (tag: string): HTMLElement => {
	// ...
}
```
### Перегрузка объявления функции
```ts
function addUser(id: string): void;
function addUser(id: number): void;

function addUser(id: string | number) {
	console.log(`Adding ${id}, ${typeof id}`);
}
addUser('vasya');
addUser(123);
```

---

## 📇 Additional Metadata
- 🛠️ Status:: #📚
- 🗂 Type:: #type/note
- ℹ️ Source:: [[Профессиональный TypeScript]]
- 🏷️ Tags:: [[Функция]]