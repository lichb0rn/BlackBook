---
uuid: 20230917200406
created: 2023-09-17T20:04:06
aliases:
  - "null"
---

# [[TypeScript null types]]

В [[JavaScript]] есть два значения для выражения отсутствия: `null` и `undefined`. TypeScript поддерживает их оба и имеет для них типы.

JavaScript-программисты обычно используют их как взаимозаменяемые, хотя между ними и есть тонкое семантическое различие, достойное упоминания: `undefined` означает, что нечто еще не было определено, а `null` показывает отсутствие значения (как если бы вы пытались вычислить значение, но сталкивались при этом с ошибкой).

Типы `void` и `never` проводят различие между несуществующими вещами: `void` — это возвращаемый тип функции, которая не возвращает ничего явно (например, `console.log`), а `never` — это тип функции, которая никогда ничего не возвращает (выбрасывает [[TypeScript Exceptions|исключение]] или выполняется бесконечно.

---

## 📇 Additional Metadata
- 🛠️ Status:: #📚
- 🗂 Type:: #type/note
- ℹ️ Source:: [[Профессиональный TypeScript]]
- 🏷️ Tags:: [[Система типов TypeScript]]