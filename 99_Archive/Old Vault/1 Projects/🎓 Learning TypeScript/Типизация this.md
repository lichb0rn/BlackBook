---
uuid: 20230920171650
created: 2023-09-20T17:16:50
alias:
---

# [[Типизация this]]

Переменная [[JavaScript this|this]] определяется для каждой функции, а не только для тех, которые существуют в качестве методов в классах. Значения `this` отличаются в зависимости от того, как вызывается функция, что может сделать ее заведомо нестабильной и трудной для понимания.

Причина нестабильности `this` связана со способом ее присваивания. `this` при вызове метода принимает значение слева от точки. Например:
```javascript
let x = { 
	a() {
		return this
	}
}
x.a()            // this является объектом x в теле a()
```
Но если в какой-то момент вы переназначите a, прежде чем вызвать ее, результат изменится:
```javascript
let a = x.a
a()              // теперь this является undefined в теле a()
```
[[TypeScript]] решает эту проблему. Если ваша функция использует `this`, обязательно объявите ожидаемый тип `this` в качестве первого параметра функции (до любых дополнительных параметров), и [[TypeScript]] проследит, чтобы `this` в каждом месте вызова оказалась тем, чем вы ее назначили. `this` не рассматривается как остальные параметры — это резервное слово, используемое как часть сигнатуры функции:
```ts
function fancyDate(this: Date) {
	return `${this.getDate()}/${this.getMonth()}/${this.getFullYear()}`
}
fancyDate.call(new Date()); // OK
fancyDate(); // The 'this' context of type 'void' is not assignable 
			// to method's 'this' of type 'Date'.ts(2684)
```


---

## 📇 Additional Metadata
- 🛠️ Status:: #📚
- 🗂 Type:: #type/note
- ℹ️ Source:: [[Профессиональный TypeScript]]
- 🏷️ Tags:: [[JavaScript this]]