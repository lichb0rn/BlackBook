
---
uuid: 20221011093933
created: 2022-10-11T09:39:33
alias:
- depth-first
- depth first
- поиск в глубину
- dfs
tags:
---

# [[Depth-First Search]]

- Начинаем с какой-то вершины и идём по какой-либо ветке до тех пор, пока не уткнемся в тупик. Далее возвращаемся и идём по следующей ветке до тех пор, пока не найдём вершину, которую ищем или не обойдём все вершины.
- Поиск отдаёт приоритет недавно добавленным вершинам.

DFS удобно использовать:
- [[Топологическая сортировка]]
- [[Определение циклов в графе]]
- Поиск пути
- Поиск соединений в разреженном графе

### Код
```swift

extension Graph where Element: Hashable {
    
    public func depthFirstSearch(from source: Vertex<Element>) -> [Vertex<Element>] {
        
        var stack: Stack<Vertex<Element>> = []
        var pushed: Set<Vertex<Element>> = []
        var visited: [Vertex<Element>] = []
        
        stack.push(source)
        pushed.insert(source)
        visited.append(source)
        
        // Проверяем, пустой ли стек
    outer: while let vertex = stack.peek() {
        
        // если не пустой, то смотрим всех соседей
        let neighbors = edges(from: vertex)
        
        // если соседей нет
        guard !neighbors.isEmpty else {
            // убираем вершину из стека и делаем ход назад
            stack.pop()
            continue
        }
        // проходим по всем соседям и смотрим
        for edge in neighbors {
            // проходили ли мы по ним
            if !pushed.contains(edge.destination) {
                // если нет. то помещаем в стек
                stack.push(edge.destination)
                pushed.insert(edge.destination)
                visited.append(edge.destination)
                continue outer
            }
        }
        stack.pop()
    }
        
        return visited
    }
}
```

   ### Производительность
   - Общая сложность алгоритма: *$O(V+E)$*
   - Затраты на память: *$O(V)$* т.к. мы помещаем все вершины в стек.
- 
---

## 📇 Additional Metadata

- 🗂 Type:: #type/note
- ℹ️ Source:: [[Data Structures & Algorithms in Swift]]
- Реализация [[Графы (реализация)|графа]]


