---
uuid: 20210712152405
created: 2021-07-12T15:24:05
alias:
- animation
- анимация
---

# [[CSS Animation]]

# Animation

Для создания анимации [[064 HTML and CSS|CSS]] используют свойства семейства `animation` и правила `@keyframes`.
`animation-name` задает имя анимации, `animation-duration` - время.
`@keyframes` определяет, что происходит в конкретный момент времени, от 0% до 100%.

```css
#rect {
	animation-name: rainbow;
	animation-duration: 4s;
}

@keyframes rainbow {
	0% {
		background-color: blue;
	}
	50% {
		background-color: green;
	}
	100% {
		background-color: yellow;
	}
}
```

Пример создает анимацию перехода от синего через зеленый к желтому за 4 секунды.

Свойство `animation-fill-mode` определяет, как нужно применить стили к элементу до/после выполнения анимации.

```css
animation-fill-mode: none;
animation-fill-mode: forwards;
animation-fill-mode: backwards;
animation-fill-mode: both;
```

- `forwards` сохранит конечный стиль
- `backwards` применит стиль первого кадра
- `both` - будет вести себя так, как будто `forwards` и `backwards` одновременно

Свойства `animation-iteration-count` определяет, сколько раз будет повторена анимация. `infinite` включает бесконечную анимацию.

```css
#ball {
	width: 100px;
	height: 100px;
	margin: 50px auto;
	position: relative;
	border-radius: 50%;
	background: linear-gradient(35deg, #ccffff, #ffcccc);

	animation-name: bounce;
	animation-duration: 1s;
	animation-iteration-count: infinite;
}

@keyframes bounce {
	0% {
		top: 0px;
	}
	50% {
		top: 249px;
		width: 130px;
		height: 70px;
	}
	100% {
		top: 0px;
	}
}
```

Пример создает прыгающий градиентный шар, который сплющивается в нижней точке.

`animation-timing-function` задает ускорение анимации:

- `linear` - постоянная скорость во время анимации
- `ease` (default) - медленно вначале, быстрее к середине и замедление к концу
- `ease-in` - медленно вначале и ускорение к концу
- `ease-out` - быстро на старте и замедление к концу
- `cubic-bezier(x0,y0,x1,y1)` - временная функция на основе [[Кривые Безье|кривых Безье]]


---

## 📇 Additional Metadata
- 🛠️ Status:: 
- 🗂 Type:: #type/note
- ℹ️ Source:: 
-  🏷️ Tags:: [[Animation]]