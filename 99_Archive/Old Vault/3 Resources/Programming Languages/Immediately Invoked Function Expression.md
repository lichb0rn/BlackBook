---
uuid: 20220717175154
created: 2022-07-17T17:51:54
alias: 
- IIFE
- Self-Executing Anonymous Function
---

# [[Immediately Invoked Function Expression]]

IIFE -  это [[JavaScript]] функция, которая выполняется сразу же после определения.
```javascript
(function () {
	statements;
})();
```

Функция состоит из двух частей:
1. Анонимная функция с лексической [[Область видимости|областью видимости]], заключенная внутри [[Оператор группировки|оператора группировки]]:
```javascript
function () {
	statements;
}
```
Благодаря этому переменные IIFE замыкаются в его пределах и глобальная области видимости не засоряется.
2. Вторая часть создаёт мгновенно выполняющееся функциональное выражение  `()` , благодаря которому JavaScript-движок выполняет функцию напрямую.

## Примеры
- Функция становится мгновенно выполняющимся функциональным выражением. Переменные внутри функции не могут быть использованы за пределами её области видимости.
```javascript
(function () {
    var aName = "Barry";
})();
// Variable name is not accessible from the outside scope
aName // throws "Uncaught ReferenceError: aName is not defined"
```

- Переменная, которой присвоено IIFE, хранит в себе результат выполнения функции, но не саму функцию.

```javascript
var result = (function () {
    var name = "Barry";
    return name;
})();
// Immediately creates the output:
result; // "Barry"
```

---

## 📇 Additional Metadata

- 🗂 Type:: #type/note
- Additional reading:: [Ben Alman's blog](https://benalman.com/news/2010/11/immediately-invoked-function-expression/)